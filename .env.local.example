# =============================================================================
# Stepease - AI Provider Configuration
# Configure multiple AI providers for automatic fallback on errors/rate limits
# =============================================================================

# -----------------------------------------------------------------------------
# Provider 1 (Google - Primary)
# -----------------------------------------------------------------------------
PROVIDER_1_NAME=google
PROVIDER_1_MODEL=gemini-3-flash-preview
PROVIDER_1_API_KEY=your_google_api_key_here

# Alternative: Use existing GOOGLE_API_KEY if PROVIDER_1_API_KEY not set
# GOOGLE_API_KEY=your_google_api_key_here

# -----------------------------------------------------------------------------
# Provider 2 (OpenRouter - Fallback 1)
# Get API key from: https://openrouter.ai/keys
# -----------------------------------------------------------------------------
PROVIDER_2_NAME=openrouter
PROVIDER_2_BASE_URL=https://openrouter.ai/api/v1
PROVIDER_2_MODEL=meta-llama/Llama-3.3-70B-Instruct
PROVIDER_2_API_KEY=your_openrouter_api_key_here

# -----------------------------------------------------------------------------
# Provider 3 (Groq - Fallback 2, optional)
# Get API key from: https://console.groq.com/keys
-----------------------------------------------------------------------------
PROVIDER_3_NAME=groq
PROVIDER_3_BASE_URL=https://api.groq.com/openai/v1
PROVIDER_3_MODEL=llama-3.3-70b-versatile
PROVIDER_3_API_KEY=your_groq_api_key_here

# -----------------------------------------------------------------------------
# Fallback Behavior Configuration (Optional)
# -----------------------------------------------------------------------------
# Enable/disable fallback system (default: true)
# PROVIDER_FALLBACK_ENABLED=true

# Max retries per provider (default: 3)
# PROVIDER_MAX_RETRIES=3

# Backoff multiplier (default: 2, results in: 1s, 2s, 4s...)
# PROVIDER_BACKOFF_MULTIPLIER=2

# Initial backoff in milliseconds (default: 1000)
# PROVIDER_INITIAL_BACKOFF_MS=1000

# Max backoff in milliseconds (default: 10000)
# PROVIDER_MAX_BACKOFF_MS=10000

# Timeout per provider in milliseconds (default: 20000)
# PROVIDER_TIMEOUT_MS=20000
